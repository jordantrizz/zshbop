# TODO

## Current

### docksoft
* Create a command called docksoft that creates a folder called containres in /root and generates common container files.
* docksoft init would create the folders /root/containers and /srv/conatiners for data.
* docksoft traefik would create a traefik container with a default configuration and a docker-compose file to run it.
* docksoft uptime-kuma would create an uptime-kuma container with a default configuration and a docker-compose file to run it.
* 

### Visual Studio Code Detect
* Print out a _loading3 "Detected Visual Studio Code shell"

### Code Comments
* Fix cmds-aws.zsh header.


## Testing

## Outstanding

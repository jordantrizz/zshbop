# Creating Github Issues with Copilot
## Create Issue
```
gh issue create --title "Log and record zshbop boot time components for improvements" --body "Tracking boot time for zshbop components when the script is loading. Write out timings into the debug, as well the ~/.zshbop.log" \
--label "area:logging,priority:p2" \
--add-assignee copilot-swe-agent
```
## Summarize Issue
```

# Outstanding
* Tracking boot time for zshbop components when the script is loading. Write out timings into the debug, as well the ~/.zshbop.log
* Look at improving the boot time for zshbop, it can vary between machines from 2s to 10s.

# Completed